import React from 'react';
import AppContainer from './components/MainNavigation';

export default class TabsView extends React.Component {
  constructor (props) {
    super(props);
    this.state = {
    };
  }

  render () {
    return (
      <AppContainer />
    );
  }
}
